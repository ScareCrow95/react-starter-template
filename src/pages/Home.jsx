import React, { useState, useEffect } from 'react'
import { Box, Text, Center, Flex, Image, Circle, Icon } from '@chakra-ui/react'
import { observer } from 'mobx-react-lite'

const Home = observer(() => {
  return <Flex w='100vw' h='100vh' bg='sec.300'></Flex>
})

export default Home
